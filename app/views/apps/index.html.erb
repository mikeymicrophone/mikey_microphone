<div id="apps_list">
	<% @apps.each do |app| %>
	  <%= div_for app do %>
			<h3><%= app.name %></h3>
		  <%= content_tag :div, :class => 'description' do %>
    		<%= textilize(app.description).html_safe %>
		  <% end %>
		  <%= link_to app.url, app.url %>
		<% end %>
	<% end %>
</div>
