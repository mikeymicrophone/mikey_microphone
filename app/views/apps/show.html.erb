<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @app.name %>
</p>

<p>
  <b>Description:</b>
  <%= @app.description %>
</p>

<p>
  <b>Url:</b>
  <%= @app.url %>
</p>

<%= link_to 'add detail', new_detail_path(:detail => {:described_type => 'App', :described_id => @app.id}) %>
<%= link_to 'new link', new_link_path(:link => {:linked_type => 'App', :linked_id => @app.id}) %>

<%= link_to 'Edit', edit_app_path(@app) %> |
<%= link_to 'Back', apps_path %>
