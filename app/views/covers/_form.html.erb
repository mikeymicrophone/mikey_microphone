<%= form_for(@cover) do |f| %>
  <% if @cover.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cover.errors.count, "error") %> prohibited this cover from being saved:</h2>

      <ul>
      <% @cover.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :band %><br />
    <%= f.select :band_id, Band.all.map { |band| [band.name, band.id] } %>
  </div>
  <div class="field">
    <%= f.label :song %><br />
    <%= f.select :song_id, Song.all.map { |song| [song.name, song.id] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
